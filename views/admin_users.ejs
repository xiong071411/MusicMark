<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= siteName %> - 用户管理</title>
  <link rel="stylesheet" href="/static/styles.css" />
  <meta name="color-scheme" content="light dark" />
</head>
<body>
  <header class="topbar">
    <div class="brand"><%= siteName %></div>
    <nav>
      <a href="/dashboard">听歌记录</a>
      <a href="/stats">统计</a>
      <a href="/settings">设置</a>
      <a class="active" href="/admin/users">用户管理</a>
      <form method="post" action="/logout" style="display:inline">
        <button type="submit" class="btn">退出</button>
      </form>
    </nav>
  </header>
  <main class="container">
    <% if (flash) { %>
      <div class="alert <%= flash.type %>"><%= flash.message %></div>
    <% } %>
    <section class="card shadow">
      <h3>创建用户</h3>
      <p class="subtitle mb-1">创建后可在下方列表中重置密码</p>
      <form method="post" action="/admin/users/create" class="grid-2">
        <label>用户名<input name="username" required autocomplete="off" /></label>
        <label>密码<input name="password" type="password" required autocomplete="new-password" /></label>
        <label>角色
          <select name="role">
            <option value="user">user</option>
            <option value="admin">admin</option>
          </select>
        </label>
        <div></div>
        <button class="btn primary" type="submit">创建</button>
      </form>
    </section>

    <section class="card mt-2">
      <h3>导入听歌记录（CSV）</h3>
      <p class="subtitle mb-1">仅管理员可见。文件需包含表头：title, artist, album, source, started_at, duration_sec, external_id（可留空）。</p>
      <form method="post" action="/admin/import/listens" enctype="multipart/form-data" class="inline" style="gap:12px;align-items:center">
        <input type="file" name="file" accept=".csv,.xlsx,.xls,text/csv" required />
        <button class="btn" type="submit">开始导入</button>
      </form>
    </section>

    <section class="card mt-2">
      <h3>用户列表</h3>
      <div class="table-wrap">
      <table class="table">
        <thead>
          <tr>
            <th class="hide-sm">ID</th>
            <th>用户名</th>
            <th>角色</th>
            <th class="hide-sm">创建时间</th>
            <th>操作</th>
          </tr>
        </thead>
        <tbody>
          <% users.forEach(u => { %>
            <tr>
              <td class="hide-sm"><%= u.id %></td>
              <td><%= u.username %></td>
              <td><%= u.role %></td>
              <td class="hide-sm"><%= new Date(u.created_at * 1000).toLocaleString() %></td>
              <td>
                <form method="post" action="/admin/users/<%= u.id %>/reset" class="inline stack-sm">
                  <input name="password" type="password" placeholder="新密码" required />
                  <button class="btn" type="submit">重置密码</button>
                </form>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
      </div>
    </section>
  </main>
</body>
</html>


