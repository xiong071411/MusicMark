<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= siteName %> - 登录</title>
  <link rel="stylesheet" href="/static/styles.css" />
  <meta name="color-scheme" content="light dark" />
  <style>body{display:flex;min-height:100vh;align-items:center;justify-content:center} .logo{display:flex;gap:8px;align-items:center;justify-content:center;color:var(--brand);font-weight:700}</style>
</head>
<body class="auth">
  <div class="card shadow auth-card">
    <div class="logo mb-1">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M9 18V6l9-2v12" opacity=".35"/><path d="M7 9v10a4 4 0 1 0 2-3.465V7.53a6 6 0 0 1 3.333-5.368l.334-.162A2 2 0 0 1 15 3.82V6l-6 1.333Z"/></svg>
      <span><%= siteName %></span>
    </div>
    <h2 class="text-center mb-3">登录</h2>
    <% if (flash) { %>
      <div class="alert <%= flash.type %>"><%= flash.message %></div>
    <% } %>
    <form method="post" action="/login" class="stack" autocomplete="off">
      <label>用户名<input name="username" required autocomplete="username" /></label>
      <label>密码<input name="password" type="password" required autocomplete="new-password" /></label>
      <label style="display:flex;align-items:center;gap:8px">
        <input type="checkbox" name="remember" value="1" />
        <span>自动登录（30 天免登录）</span>
      </label>
      <button type="submit" class="btn primary">登录</button>
    </form>
    <p class="subtitle mt-2 text-center">默认管理员账号在首次启动时会自动创建</p>
  </div>
</body>
</html>


